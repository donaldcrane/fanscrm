const path = require('path');

module.exports = {
    'config': path.resolve('src', 'database', 'database.provider.js'),
    'migrations-path': path.resolve('src', 'database', 'migrations'),
    "debug": true
};
